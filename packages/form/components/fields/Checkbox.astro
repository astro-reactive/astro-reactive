---
import Field from '../Field.astro';
import { FormControl } from '../../core';

export interface Props {
    control?: FormControl
    controlName?: string
    showValidationHints?: boolean 
}


const { control, controlName, showValidationHints = false } = Astro.props;

let controlPassword: FormControl = new FormControl({
    ...control?.copy(),
    type: "checkbox",
    name: controlName ?? control?.name ?? "",
})
---

<Field showValidationHints={showValidationHints} control={controlPassword}/>